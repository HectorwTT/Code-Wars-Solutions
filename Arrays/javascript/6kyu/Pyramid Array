function pyramid(n) {
  // your code here
  const result=[];
  for(let i=1;i<=n;i++){
    const arr=[];
    for(let j=1;j<=i;j++){
      arr.push(1);
    }
    result.push(arr);
  }
  return result;
}
