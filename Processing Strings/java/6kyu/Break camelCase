class Solution {
  public static String camelCase(String input) {
    String output="";
    for (int i=0;i<input.length();i++){
      char ch = input.charAt(i);
      if (ch>=65 && ch <=90) output +=" "+ch;
      else output +=ch;
    }
    return output;
  }
}
